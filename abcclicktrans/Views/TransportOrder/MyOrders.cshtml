@using abcclicktrans.ViewModels
@model IEnumerable<abcclicktrans.ViewModels.TransportOrderViewModel>
@{
    ViewData["Title"] = "Moje zlecenia";
}
<div class="container pb-40">
    <div class="panel">
    @foreach (var item in Model)
    {
        <details>
            <summary>
                <ul>
                    <li class="name">@item.Name</li>
                    <li class="titleValue"><time>Odbiór:</time> @item.PickUpAddress.City</li>
                    <li class="titleValue"><time>Dostawa:</time> @item.DeliveryAddress.City</li>
                </ul>
            </summary>
            <div class="content">
                <div class="row">
                    <div class="col-md-3 col-sm-3">
                        <a href="@item.ImageSrc" class="img-pop-up">
                            <img src="@item.ImageSrc" alt="Obraz" class="img-thumbnail">
                        </a>
                    </div>
                    <div class="col-md-5 col-sm-5">
                        <span class="text-justify text-dark">@item.Description</span>
                    </div>
                    <div class="col-md-4 col-sm-4">
                        <span>Zleceniodawca:</span><br/>
                        <span class="txt"><b>Email: </b>@item.User.Email</span><br/>
                        @if (item.User.PhoneNumber != null)
                        {
                            <span class="txt"><b>Telefon: </b>@item.User.PhoneNumber</span>
                        }
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6 col-sm-6">
                        <p><b>Adres nadania:</b></p>
                        <span class="text-justify text-dark">
                            Ulica: @item.PickUpAddress.Street<br/>
                            Miasto: @item.PickUpAddress.Postal @item.PickUpAddress.City<br/>
                            Kraj: @item.PickUpAddress.Country<br/>
                        </span>
                    </div>
                    <div class="col-md-6 col-sm-6">
                        <p><b>Adres dostawy</b></p>
                        <span class="text-justify text-dark">
                            Ulica: @item.DeliveryAddress.Street<br/>
                            Miasto: @item.DeliveryAddress.Postal @item.DeliveryAddress.City<br/>
                            Kraj: @item.DeliveryAddress.Country<br/>
                        </span>
                    </div>
                </div>
            </div>
        </details>
    }
    </div>
</div>


@*<div id="accordion">
    @foreach (var item in Model)
    {
    int c = 1;
    string h = "heading", coll = "collaps";
    <div class="card">
    <div class="card-header" id="@(h+c)">
    <h4 class="mb-0">
    <button class="btn btn-link" data-toggle="collapse" data-target="#@(coll+c)" aria-expanded="true" aria-controls="@(coll+c)">
    @item.Name
    </button>
    </h4>
    <h5>
    Odbiór: @item.PickUpAddress.City  |  Dostawa: @item.DeliveryAddress.City
    </h5>
    </div>
    <div id="@(coll+c)" class="collapse show" aria-labelledby="@(h+c)" data-parent="#accordion">
    <div class="card-body">
        <div class="row">
            <div class="col-md-3 col-sm-3">
            <img src="@item.ImageSrc" alt="Obraz" class="img-thumbnail">
            </div>
            <div class="col-md-5 col-sm-5">
            <span>@item.Description</span>
            </div>
            <div class="col-md-4 col-sm-4">
            <span>Zleceniodawca:</span><br/>
            <p><b>Email: </b>@item.User.Email</p><br/>
            @if (item.User.PhoneNumber != null)
            {
            <p><b>Telefon: </b>@item.User.PhoneNumber</p>
            }
            </div>
        </div>
        <div class="row">
            <div class="col-md-6 col-sm-6">
            <span>Adres nadania:</span>
            </div>
            <div class="col-md-6 col-sm-6">
            <span>Adres dostawy</span>
            </div>
        </div>
    </div>
    </div>

    </div>
    }
    </div>*@